# Working notes for Claude

Read this first in every session. Sessions do not share memory; this file and `docs/DESIGN.md` are the continuity.

## Status

- **Done:** Phase 1 step 1 (map, supply drops, wall bar, placement with path preview, undo/pick-up, waves, camera). Phase 1 step 2: Twin tower (1×1) and its Gatling form (2×2) on walls, credits with flat income, selling (full refund in the phase built, 75% after, also mid-wave), enemy HP with growth, nexus HP and run over/restart, tuning panel (K) saved in localStorage. Turret look picked by Erik: design B "Twin" from `mockups/turrets/`.
- **New direction (2026-09-25):** survival tower defense. Explore a planet, mine, build a base, defend it by mazing. Read `docs/PLAN.md` first: agreed decisions, milestones M0 to M5, idea bank, open questions. The old Phase 2+ roadmap in `docs/DESIGN.md` is superseded.
- **Done (M0):** Armored deck walls in the game (per-piece model `wallPiece`, outline logic in `src/render/pieceShape.ts`), design doc rewritten for the survival direction.
- **Ship:** the Rocket (`mockups/rocketship/`, https://claude.ai/artifact/GBVZ8RpZPQaqY44reLU2hg): 3×3, three fins (one front-centre), Reactor core in an open cage mid-body, wider lower body as a sturdy base, nose cone directly on the core cage (no upper cylinder), cargo bay front-left of the front fin with a door that becomes the ramp (shut when landing, opens after touchdown), fabricator with console screen front-right, both at walking height. The player arrives earlier by drop pod, never from the ship. Waiting on Erik's final OK.
- **Rocket is final** and lives in `src/render/ship.ts` (model `ship`, `userData.rig` has the door, bay and core).
- **Settled for M1a:** mining is a held key (not automatic). Order: avatar, then ore, then fabricator.
- **Done (M1a part 1):** the Rocket replaces the nexus (3×3 on Frostfall, lands at the start of a run, ramp opens); the player rig runs with WASD (screen-relative), jumps with Space onto wall decks, can climb onto towers from a wall deck (tower tops in `TOWER_INFO.top`), is blocked by rocks, trees and the ship, never blocks enemies; walls and towers can't be placed on it. Follow camera (drag or arrows pan, C follows again, H glides to the ship). P pauses (freezes the rig too), V toggles the path preview. Game speed (F) never affects the rig: `Game.stepAvatar` runs on real time, `Game.step` on game time. Rig drawn between sim ticks, shadows snapped. Tool raised while holding a wall or tower.
- **Ore look:** Erik picked B1 (ore body) from `mockups/ore-b/`, without the crater: a node sitting on the snow, two kinds with the same shape: stone (plain grey, no flecks) and metal (whole node steel-silver). `createOreNode(n, seed, kind)`. One node size only: 3×3 (like Rust; 2×2 dropped for simplicity). A stone node holds 600 stone (200 per stage), a metal node 300 metal (100 per stage); a node takes ~8.3 s to mine out either way (~2.8 s per stage). Model in `src/render/ore.ts` (`createOreNode(n, seed)`, `setAmount` shrinks it). Ore playground: `mockups/ore-play/` (https://claude.ai/artifact/WoEZJHDMM4D1tzaozyKevD): 2×2 and 3×3 nodes, hold left mouse to mine (you can keep moving and jumping while mining: legs run where you steer, the torso twists up to ~110° toward the aim; the mouse button always fires the tool, aiming at the cursor or at the node in reach, and only mines when a node is in reach), nodes are solid, and their blocking footprint shrinks per stage (3×3, then a plus of 5 cells, then the centre cell); mining reach is a 1.5-cell gap around the full 3×3 footprint measured in screen space (the camera squashes depth by half, so a world circle looked shorter above/below than at the sides), the same-looking on every side and at every stage. Nodes break in three stages like Rust (outer rocks at 2/3, middle at 1/3, core at 0, with chunk bursts), not a smooth shrink; respawn after 20 s in the playground (respawn in the game is a later decision).
- **Done (M1a part 2, in the game):** everything from the ore playground, plus the build wheel (look A). Rules: `src/sim/ore.ts` (nodes, stage footprint, `viewGap` reach, `VIEW_SQUASH`), `Game.nodes`, `Game.hotbar`, `Game.mineInput` (set by the controller: firing, onSpot), `Game.mining` (node, full), mining in `stepAvatar` (real time), `node-broke` / `node-grew` events, regrowth at the start of planning (clear 3×3, no wall, not the avatar, path stays open). Nodes block enemies and the avatar (`World.ore`). Walls cost `tuning.wallCost` stone per cell (refunded on pick-up), towers cost alloy since the smelter (Twin 200, Gatling 500); start 400 stone, 150 metal; credits and income are gone. Frostfall: no ore within `SHIP_CLEARANCE` (4) cells of the ship (tested); 3 stone a short trip out (east, south-east, south), 2 metal further toward the rift side. Jumping: rocks (top `rockTop(h)`, 0.64 to 0.96) and ore nodes (a mound, `nodeCellTop`: 0.6 corners, 0.78 edges, 0.95 peak) can be jumped onto; trees are hurdles (`TREE_HURDLE` 0.7, not standable: `Game.standable`; land on one and `Avatar.depenetrate` slides you off); only the ship is solid. Take off about a cell before a tree to clear it. The avatar collides in eighths of a cell (`AVATAR_SUB`, `Game.heightAtFine`): a tower leaves a 1/8-cell rim of wall deck on its outer sides, so you can jump from the snow onto the rim and from there onto the tower. Blueprints: the wall ghost is the stone wall shape (`stoneWallPiece`) in see-through grey, red when it can't go there (every wall is placed as stone); tower ghosts are colony orange when placeable, grey with a red footprint when not. Render: `src/render/mining.ts` (`MiningView`: node models, bursts, glint, sparks, aim), torso twist in `GameView.updateAvatar`. Input: LMB fires the tool when nothing is held (a short press is still a click: select a tower, pick up a wall), middle-drag pans, and LMB drag pans too unless the press starts next to a node with the multitool out (then it mines; `drag.mining` in the controller), Q/E wheels (`src/ui/buildWheel.ts`), 1–6 hotbar, Shift sprint; after placing a wall you keep holding the same shape while you have more. HUD: hotbar, "+N"/Full popup, supply notice with shape icons; the wall bar, tower cards and credits are gone. Tuning saved under `risen.tuning.v3` (old saved tuning is dropped when the key changes). The ore playground still has its own copy of the mining effects.
- **Stone and metal walls (in the game):** every piece goes down as stone (look C, `stoneWallPiece`); hold right mouse on a wall for its modification wheel (centred on that wall, `Controller.openMods`) with Metal plating (`Game.plate`, `tuning.platingCost` 100 metal per piece, locked walls too, mid-wave too), which turns the whole piece into the Armored deck; towers need every footprint cell on a plated wall (reason `stone-wall`, "Needs metal plating"). Pick-up refunds stone and plating metal. Walls fuse only with the same material. Unlocked stone walls breathe and highlight like the metal ones (`stoneWallMaterials`). Start metal 150 (after the doubling a first tower costs 300, so it needs a metal trip; the ship's gun covers early waves). Wall playground: `mockups/walls/` (https://claude.ai/artifact/EUvgC91NyjTzjPR46CfRrz). Tests use `tests/helpers.ts` `metalWall()` to put plated walls down directly.
- **Ship's gun (in the game):** the ship fires a weak gun from its core crystal (`tuning.ship`: damage 1, 1 shot/s, range 5.5 from the ship's centre, same most-progress targeting as towers via `Game.pickTargetFrom`; shots carry `SHIP_SHOOTER` = 0 as their shooter; cyan bolts in the view). Click a turret or the ship for its range ring and a stats panel in the bottom-left corner (towers: plus Sell); clicking again hides it. Clicks on turrets and walls look at turret/deck height first, so you click what you see; the ship counts anywhere over its footprint. Tuning saved under `risen.tuning.v3`.
- **World playground:** `mockups/world/` (https://claude.ai/artifact/AJkZJ1LAQLTcHhgrTkE7Yv). Agreed biome direction (Erik, 2026-09-25): terrain blocks enemies and feeds the maze; generated worlds with strong rules; terraforming parked; about a minute's run from ship to the rift wastes (tune); alien violet crystal in the wastes. Built in layers, each checked on Erik's machine with the performance readout (` key: fps, draws, triangles). Layer 1 done: zones around the landing site (`zonesAt`: clearing, pine forest belt, rocky highlands with metal, violet rift wastes to the north-west with crystal fields, dead pines and two rifts), zone-coloured ground (vertex colours per chunk), ore placed by zone, world 224×224 built in 16×16 chunks merged with `bakeStatic` (about 200 draws at the default zoom). Playground zoom goes to 70 as a design overview (snow fades out there). New props: `src/render/alien.ts` (`crystalCluster`, `deadTree`). After Erik's first walk: trees and crystals block the player only at the trunk (middle half of the cell, collision in eighths of a cell) so forests can be walked through while they'd still block enemies as whole cells; loose rocks cut way down (unminable clutter), nodes about tripled (up to 130, 6+ cells apart: stone in forest and highlands, metal mostly in highlands). Snow rule (Erik): snow on the ground is always pure white and cozy; variety comes from bare patches where it's gone, each with a cause (`bareAt`): frozen lakes (ice, flat and open, no scenery), the ship's scorched landing ring, rift heat (violet-dark earth, strongest around rifts), and wind-scoured highland rock (no needle floor: Erik wants the snow to reach under the pines; rift heat and rock may need more polish). Patches are crisp low-poly shapes (marching squares at half-cell steps, a pale rim then the ground colour), one mesh per chunk. Footprints: the rig leaves prints in snow (not on bare ground or ice) that fill back in within about 8 s (one instanced mesh). Idea: warm colony buildings could melt a little bare ground around them. Layer 2 (reworked with Erik): cliffs are the edges of raised ground, not standalone walls. A smooth elevation field is raised where it passes a zone threshold (low in the highlands, middle in the wastes, high in the forest; never near the landing site or rifts), smoothed (cellular automaton), scraps under 40 cells dropped and holes under 30 filled, so plateaus are large solid unusable high ground to build around (1.35 high, out of jump reach). Plateau edges use cliff look B (tilted slabs with even settled snow, Erik's pick from `mockups/cliffs/`, https://claude.ai/artifact/Xavy4sEzsSwNPxwYLMDzUS); the inside is a flat snow top (one quad per cell, merged). Loose unminable stones removed from the world. Rift reachability check and the Path button still apply. `window.lookAt(x, y)` and `window.plateaus` exist for headless checks. Next: landmarks, then polish (rift heat, rock). Watch for the game later: a big world makes flow fields bigger (limit the pathing area).
- **Enemy spawners = cave exits (Erik, 2026-09-25):** where enemies climb out of the ground. Erik's pick: look **A** (outcrop of rough stacked rock, flat snow on every ledge, a dark mouth under an icicled overhang), `caveLook("A")` in `src/render/caveLooks.ts`; mockup `mockups/caves/` (https://claude.ai/artifact/Tafs4dH2eZawJqtYMok94x). No violet, no glow (Erik).
- **New basic enemy (Erik picked C, the leaper: no eyes, no horns or back spines, no small chest claws, head tilted down in line with the back):** Tyranid-inspired, one very dark red tone across the whole creature (#1e0406, was dark purple; matte, smooth shading, faint emissive) except the blades and tail stinger, which are bone white (#e9e1cf), with rounded shapes (capsule limbs, smooth plates, bevelled blades) so it reads organic, not icy or glassy (Erik). Feet stay on the snow: each frame the body is raised or lowered so the lowest foot tip rests on the ground (`Leg.tip`), eased (not snapped) so the walk is smooth; bob ~0.02 cells (was 0.10). Size 0.6 (`root.scale` for C, was 0.5; grounding works in root-local units), long slow strides (swing 0.62, cadence 5.5). Mockup: leapers climb out of cave A every 1.1 s, walk out in a line at 1.5 cells/s and die 7 tiles out: they simply burst in a small spray of red dots (10, smaller and closer) and are gone (Erik: simple, red). `src/render/enemyLooks.ts` (`enemyLook(look)` → object + `update(t, walking)`): A gaunt (hunched biped, digitigrade legs, two scything blades, crested head, tail), B crawler (six legs, segmented back, mantis blades), C leaper (long spring legs, spined ridge, blades plus small claws, stinger tail). Mockup `mockups/enemies/` (https://claude.ai/artifact/C48Q6rcVmBz3WzaMkWY6cR): a pack walks out of cave A past the rig; `window.closeUp()` for a close look. Model only, no role or stats yet.
- **Done (world, caves, leaper in the game):** the game runs on a generated world (`src/sim/worldgen.ts`, `generateWorld(seed)`; seed from localStorage `risen.world.seed`, default 1, separate from the World playground's storage): zones, plateaus with cliff look B, ore by zone, pines, dead pines, crystal, bare patches, drifts. Scenery is built by `src/render/scenery.ts` (`buildScenery`, chunked and baked). Enemy spawners are caves (cave look A, `src/render/caveLooks.ts`, `CaveDef {x, y, dir}` in the map): anywhere on the map except raised ground, at least `CAVE_MIN_DIST` (24) from the ship and `CAVE_SPACING` (20) apart, facing the ship, rarer than ore (about 14 vs 130); ore is about three stone nodes per metal node (metal chance 17% in the forest belt, 27% in the highlands: `METAL_FOREST`, `METAL_HIGHLANDS`), so metal turns up within about 15–30 cells of the ship; every cave must reach the ship (a pass is carved through trees or plateau, never ore, else the cave is dropped). Only the nearest `tuning.activeCaves` (3, slider "Caves sending enemies") send enemies (`Game.activeSpawners`). Trees, dead pines and crystal block enemies as whole cells but the player only at the trunk. Pines and dead pines never stand in touching cells (Erik: at least one free tile all round each tree; `treeNear` in worldgen, tested), so enemies can thread through forests between trees; crystal can still cluster. The enemy is the leaper (`enemyLook("C")` in `src/render/enemyLooks.ts`, from `mockups/enemies/`): dark red, bone-white blades and stinger, no eyes, rises out of the cave mouth, bursts into red dots on death. Pathfinding runs on a Uint8Array blocked grid (`World.blockedGrid`) with a typed-array heap (7 to 9 ms on the big world). Headless hooks: `window.perfInfo`, `window.lookAtCell(x, y)`. Watch: each leaper is ~25 meshes, so a wave of 24 costs ~700 draw calls.
- **Walls are bought with stone (Erik, 2026-09-25):** supply drops are gone (no hand, no notice, no "Walls per round" knob). The Q wheel always shows all 7 shapes, greyed when you can't afford one; placing pays `Game.shapeCost` (4 cells × `wallCost`, 100 stone by default) via `Game.place(shape, rot, at)`; you keep holding the shape while you can afford another; pick-up (`pickUp` returns the shape) refunds the stone and you hold that shape again. The planned fabricator queue (next 3, hold, reroll) is superseded unless Erik brings it back.
- **Enemy packs (Erik, 2026-09-25):** each active cave sends its wave (`waveSize(round)` enemies per cave, unchanged) in packs of `packMin`–`packMax` (3–5), climbing out `PACK_STAGGER` (0.25 s) apart, `packGap` (4 s) between packs (`Game.sendPack`, `packQueue`). Speed varies ±`speedSpread` (12%) around `ENEMY_SPEED` (1.475 cells/s), rolled once per pack (Erik: a swarm pack moves as one), so every leaper in a pack walks at the same speed and packs differ. Each walker has a `lane` (-1..1); the view offsets it sideways by `lane × laneSpread` (0.2 cells) off its tile's centre line, turning with its eased heading. Visual only: paths, range and targeting stay on the centre line. All five are Tune sliders (Enemies). Walkers are drawn between ticks (`Walker.px/py`) and animated on the world clock every frame, so the walk is smooth at any refresh rate. HP bars are red.
- **Name:** the game is called Frostfall (title and top-left brand; Risen was dropped 2026-09-25). The repo, package and localStorage keys keep the old `risen` names.
- **Smelter (in the game; was "refinery"; Erik, 2026-09-25):** raw metal in, alloy out, 1:1. Items: mined metal is **raw metal** (item `"metal"`, only good in a smelter); **alloy** (item `"alloy"`, icon: one plain ingot, no glow or sheen (Erik), `alloyIngot()` rendered in `itemIcons`, framed with room so the edge fade never touches it) pays for towers, metal plating, and comes back from selling and plating refunds. Runs start with 400 stone, 0 raw metal, 150 alloy. Rules in `src/sim/smelter.ts` (`Smelter`: 2×2, `input`/`output` are `Inventory`s of 2 stacks, `smelt()` at `tuning.smeltRate` 5/s) and `Game.checkSmelter/buildSmelter/smelterAt/canUseSmelter/smelterPut/smelterTake` (`SMELTER_REACH` 1.6 cells gap). Built from E → Buildings → Smelter: the E wheel has Twin, Gatling and a Buildings slice; pointing at Buildings swaps it for the buildings wheel (just the Smelter); release picks. Costs `smelterStone` 500 + `smelterMetal` 300 raw metal. Open ground only, blocks enemies like a wall (`World.buildings`, in `isBlocked` and `blockedGrid`), can't cut the path, is solid for the avatar; the placement preview shows the new route. Click it when you're next to it to open its panel above the hotbar (Rust furnace style: 2 raw metal slots, a flowing arrow while it works, 2 alloy slots); with it open, click a raw metal stack in the hotbar to put it in, click a smelter slot to take it; walking away or Esc closes it. The panel's Remove button takes the smelter down for its full price (`Smelter.paid`, what was actually paid) plus everything inside it (`Game.removeSmelter`: only when you're next to it, any phase; refuses with "Not enough room in the hotbar" if it can't all fit, so nothing is lost). The panel's slots stay in place and only their contents update (it smelts several a second, so rebuilding would eat clicks). Model: look A trimmed to the furnace on 2×2 (`refineryLook("A")` in `src/render/refineryLooks.ts`, turned 45° so the framed window faces the camera); its window glows and the chimney smokes only while smelting (`update(t, working)`). Mockup `mockups/refinery/` (https://claude.ai/artifact/Xaut1sxwRYzyBk6Wfjjxh7). Tuning moved to `risen.tuning.v4` (old saved values dropped: "starting metal" now means raw metal). Next (agreed): enemies attack buildings (the smelter first).
- **Raids on a clock (M1b step 1, done; Erik, 2026-09-25):** no Start wave button (and no Enter key). `Game.raidIn` counts down in game time while calm (`phase` stays "planning"/"wave" in code; the HUD says Calm/Raid and "Raid N"): `raidGrace` 240 s before the first raid, `raidInterval` 180 s after each cleared raid, and the last `raidWarning` 60 s are fixed (`raidWarned`, event `raid-warning`). Activity makes noise (`Game.noise(s)`, never into the warning): each mined stage `noiseStone` 4 / `noiseMetal` 6 s, each wall piece `noiseWall` 2, each tower or building `noiseBuild` 5, and the clock runs `smeltNoise` (50%) faster while any smelter works. At 0 the raid starts (`startWave`, still used by tests). HUD: the raid clock bottom right ("Raid in 3:42", red pulse in the warning, "Raid 12 left" during a raid); during the warning and the raid a red chevron marks each active cave, pinned to the screen edge and pointing at it when off screen (`Hud.markCaves`, clear of the top bar and hotbar). View: in the warning the active caves tremble and breathe dust from their mouths (`GameView.stirCaves`, caves keyed by mouth cell). Tune section "Raids" has all the numbers. The agreed direction for what comes next (targets vs obstacles, walls as slow obstacles, the ship can be destroyed, upkeep and decay later, towers break with their wall) is in DESIGN "Raids" / "Enemies and pathing" and PLAN M1b.
- **Targets and building HP (M1b step 2, done):** `World.targets` holds the cells enemies go for: the ship (until destroyed) and every smelter. The flow field starts from all target cells, so enemies head for the nearest; the ship's cells now block walking (`isBlocked`/`blockedGrid` include the nexus), so routes end beside it. A walker arriving next to a target (`World.targetNextTo`, 8 ways) stops and claws it (`Walker.attacking`, `tuning.enemyDamage` 2/s, `damageTarget`); practice walkers just vanish there. Ship HP `Game.hp` (`startHp` 400, float); smelter `hp`/`maxHp` (`smelterHp` 150). Ship at 0: `shipDown`, event `ship-destroyed`, nexus cells leave the targets but stay blocked (a wreck), the ship's gun goes silent, the run goes on (the "over" phase is no longer entered; the HUD shows a "ship was destroyed" notice with New run). Smelter at 0: removed with its contents (`smelter-destroyed`, grey debris burst). With no targets left, walkers burrow (removed), so a raid still ends; placement path checks are skipped then. View: attacking walkers stand, face the target and lunge; HP bars over damaged buildings (`syncBuildingBars`); the core flashes while clawed; the wreck slumps, tilts, goes dark and smokes. HUD "Ship N" flashes on damage. Tuning moved to `risen.tuning.v5`.
- **Breakable walls (M1b step 3, done):** walls are slow obstacles, and **a piece breaks as a whole** (Erik: the gap then always fits the same shape again). HP is per piece (`World.pieceHp` by piece id; stone `tuning.wallHp` 600, plating multiplies by `platedHpMult` 3 keeping the share of damage). Pathfinding grid is 0 open / 1 solid / 2 wall (`World.blockedGrid` returns `{ grid, cost }`); stepping into a wall cell costs its piece's chew time as walking distance (`World.hpToCost` = speed / (`wallClawers` × `enemyDamage`), set by `Game.syncWallWeights` every tick; `defaultWallHp` for walls set directly in tests). Diagonals can't cut a wall's corner. So enemies walk a maze when it's quicker and chew a full block otherwise; a damaged piece is cheaper (field refreshes when walls break or are repaired, not every damage tick). Walkers whose next step is a wall stop and claw it (`attacking` = wall cell key); at most `wallClawers` (2) do damage per piece per tick; they stop if it's no longer on their quickest way. `Game.breakPiece`: every cell of the piece goes, any tower on those cells is destroyed (`tower-destroyed`), event `wall-broken` (debris along the whole shape). Placement: walls may fully enclose anything (preview fields treat the new piece as a wall: `computeField(..., extraWallHp)`); seal/trap checks only trip on terrain now. `Game.pieceHp`, `repairCost` (share of the wall price for missing HP, stone), `canRepair`, `repair` (any time, from the wall's right-click wheel: Plating, Repair). View: HP bars over damaged pieces. `window.game` exposes the game for headless checks.
- **Supply (M1b step 4A, done):** `GameOptions.supply` (on in `main.ts`, off by default so older rule tests can put walls anywhere). `Game.supplied` = wall/building cells reached by a flood from the ship's cells through touching walls and buildings (8 ways, corners count), within `tuning.supplyRadius` (40) of the ship's centre (`inSupplyRange`); nothing when the ship is down. Recomputed with the flow field in `Game.refresh()` (every structure change). Placement (walls and smelter): every cell in range (reason `out-of-range`, "Too far from the ship") and at least one touching a supplied cell or the ship (`unconnected`, "Must connect to your walls"). `pieceSupplied(piece)`. View: a cyan ring at the radius while `toolReady`; unsupplied pieces get a cold, dim material. Tests in `tests/supply.test.ts`.
- **Ship inventory, upkeep and decay (M1b step 4B, done):** `Game.shipStore` (`Inventory(SHIP_SLOTS = 24)`, starts with `shipStartStone` 200; lost when the ship is destroyed). `canUseShip()` (gap to the ship's footprint ≤ `SMELTER_REACH`), `shipPut(hotbarSlot)` (anything but the multitool), `shipTake(slot)`. `upkeepPerMinute()` = `upkeepRate` (3%) of each supplied thing's price: stone for walls (`paid`) and smelters (`paid.stone`), alloy for plating (`plated`) and towers (`paid`); `upkeepLasts()` in seconds. `stepUpkeep` (in `step`, only with the supply rule): owed amounts add up and whole pieces are taken from the ship; if it can't pay, `upkeepPaid` goes false. Decay: every piece that isn't supplied, or everything while unpaid, loses `max / decayTime` (300 s) HP per second and breaks at 0 (`breakPiece`); smelters the same (destroyed at 0). UI: click the ship when next to it for its 24-slot panel above the hotbar (header "Upkeep N stone · M alloy / min · lasts m:ss", red when unpaid or under a minute); with it open, click any hotbar stack to put it in, a ship slot to take it; from further off a click still shows the gun's range and stats. Top bar: red "Upkeep unpaid" pill. Tests in `tests/upkeep.test.ts`.
- **Next: M1b step 4C**, the hub (a buildable ship-like supply source with its own area and inventory, buildable anywhere; mockups first). Ask Erik before building.
- **Hotbar (ore playground, into the game next):** Rust/Minecraft-style, 6 slots at the bottom centre, the only inventory for now (`src/sim/inventory.ts`, `Hotbar`, tested). Slot 1 holds the multitool; keys 1–6 select; the tool only fires with the multitool selected. Ore arrives in whole chunks, one per stage that breaks off (stone 200, metal 100 per stage), never ticking up while mining; it stacks into slots (stack 1000, tops up its own stack, then the first empty slot); if the next chunk does not fit, mining does nothing (the node keeps its ore, the popup shows Full). The stone/metal counter was removed; a "+N" with the ore icon pops over the node when a stage breaks and fades after 1 s. Ore icons are the node's last stage (its core rock) rendered from the real model at startup with the world's evening lights (sun ×1.9, sky ×1.05 so they pop on the dark hotbar; the metal icon alone gets a polished copy of its material that mirrors a dark room with bright light strips (`stripRoom()`), so some facets flash bright: that is what reads as silver; a plain white room just looked white) and the sun's shadow on (invisible) snow, faded at the edges (`itemIcons()` in `src/render/icons.ts`, which also renders the multitool icon side on from `createMultitool()` in `rig.ts`). Counts show as x200 (stack max 1000 for stone and metal). Selected slot outlined in colony orange; no slot numbers (no control hints).
- **Build wheel (Erik picked look A; in the game):** hold Q for walls (the 7 shapes, greyed and unpickable when you can't afford the stone), E for towers (Twin, Gatling; greyed if you can't afford their metal). Centred on the character, never the mouse; stays up only while the key is held; the centre picks nothing; each slice is an invisible cone running out past the wheel, so pointing outside still picks by direction; releasing picks the highlighted item (then LMB places, R / right-click rotate, Esc or 1 drops it). No costs shown in the wheel yet. The wheel replaces the wall bar in the game; supply drops only show a short notice like "+1 T +2 L". Slice maths in `src/ui/wheel.ts` (tested); looks in `mockups/ore-play/wheel.ts`. The approved game plan for M1a part 2 (hotbar, ore costs, nodes, wheel) is in `docs/PLAN.md`.
- **Sprint (in the playgrounds, game next once Erik has tuned it):** hold Shift, top speed × `AvatarTuning.sprint` (default 1.4, so 5 → 7 cells/s); no stamina; not while the tool is raised (mining or building), so sprint is a travel mode; jump height unchanged, sprint speed carries into the air. The rig blends a longer, wider stride with more forward lean above run speed (`RigAnimator` sprintW). Playground slider "Sprint" is appended last so saved values keep their order.
- **Movement playground:** `mockups/playground/` (https://claude.ai/artifact/3R3F7P5FTggg1WcXbAVQsR). Uses the real `src/sim/avatar.ts` (pure movement rules, tested in `tests/avatar.test.ts`) and `src/render/rig.ts` (rig model + `RigAnimator`, which blends idle/run/air/landing from motion). Erik is tuning the feel there (his values live in his browser's localStorage; he copies them with the panel's Copy values button); Erik's tuned values are now the defaults (speed 5, stride 2 after in-game feedback; accel 40, turn 20, jump 0.9 / rise 0.35, air control 0.5, camera follow 4). No motion blur, ever (Erik dislikes it). Snow lives in world space and wraps around the camera area; the field is wide enough for the most zoomed-out view and always falls the same way; zoom only changes how many flakes are drawn (setDrawRange, fewer when zoomed out) so it looks equally dense on screen. Never move or speed flakes because of the camera (Erik: snow just falls). Moving things must be drawn interpolated between 60 Hz sim ticks and animated per rendered frame, and the shadow camera snapped to its texel grid; otherwise the rig vibrates (fixed in the playground, carry into the game in M1a).
- Avatar history: Erik picks an avatar. First round (`mockups/avatar/`, https://claude.ai/artifact/H5ECqtwJGetKCkiSGX224s) didn't land; Exo-rig was the right direction. Exo-rig round (`mockups/exorig/`) also missed; Erik wants a humanoid exo-rig with plain labels, no themed names. Humanoid round (`mockups/humanoid/`): B was closest. B variations (`mockups/humanoid-b/`): Erik chose B2 and asked for revisions (no shoulder pads, torso = hip width, backpack flush with torso top, square helmet with visors), then: no helmet stripe, no side visors, smaller head, short neck; then: no ore canisters on the pack, hips pulled back, no drill or fingers, a blocky prefab multitool (tool gun with cyan beam and a raked pistol grip) in the right hand, carried relaxed when idle or walking and raised only when mining or building. Rig is approved as the player character. Jump added (mockup Jump / Wall hop): the rig can jump onto wall decks and run along them; it never blocks enemies. M1a part 1 plan approved. Space = jump, P = pause. The rig always runs (no walk cycle); movement speed will go up later. No chest plate; smaller backpack. Colors: white armor, dark steel limbs and joints, orange bands, cyan visor and core (a wall-palette recolor was tried and reverted). Rig look is final. Gotcha: create materials lazily, never at module import; THREE.ColorManagement is switched off at startup and materials made earlier get their hex colours converted (orange turned red). Torso pivots at hip height over a pelvis that belongs to the hips; the multitool is rigid on the wrist, pointing along the forearm. Revised rig: `mockups/rig/` (https://claude.ai/artifact/6tc7ay9BZja5htwtdCNBCg); walks in place on a turntable. Note: roundedBox bevels bulge past w/d, so face details need the `bulge()` offset. Then M1a part 1 (avatar + Rocket in the game), described to Erik before building.
- Picks so far: walls = Armored deck, nexus/ship = Reactor core as starting point (`mockups/stronghold/`, published at https://claude.ai/artifact/DkXNNFtuUA26uCJPa3sLa5).
- **Run locally:** double-click `start-dev.cmd` (Windows) or `npm install && npm run dev`, then http://localhost:5173.

## Collaboration

- Erik is the designer and player; Claude writes the code. Erik's calls on design are final.
- Erik dislikes popups that interrupt play: prefer non-blocking notices.
- No instructional text in the UI ("Press Space to resume", control hints, how-to lines). It's Erik's own game; he knows the controls. Show state, not instructions.
- Take it slow on design: discuss fundamentals, reason through trade-offs, don't jump to building without agreement.
- Design: `docs/DESIGN.md` (snapshot in the repo). Erik's live, editable copy: https://claude.ai/code/artifact/8a4e11d2-eea6-4144-9f29-23a844c9d1ec. When a design decision changes, update `docs/DESIGN.md` in the same commit.
- **Standing guardrail:** every shop/economy system must feed back into maze decisions. Flag it whenever a feature risks the economy becoming the main game.

## Code conventions

- TypeScript strict, three.js for rendering, no UI framework.
- Keep game rules (grid, pathfinding, placement, combat) as pure logic, separate from rendering and input, so it can be unit tested.
- Tests live in `tests/`, run with `npm test`. Pathfinding and placement rules must have tests.
- Before committing: `npm run typecheck && npm test`.
- Small, focused commits with clear messages.

## Delivering builds

Mockups: `node scripts/mockup.mjs <name>` builds `mockups/<name>/` (can import the game's models) into `dist-mockup/<name>.html`.


`npm run build:artifact` produces `dist-single/artifact.html`, a self-contained build. Publish it with the Artifact tool to the existing playable link: https://claude.ai/artifact/44JH12KkM2JYyTDMCxxmJ4 (pass it as `url` from a new session).

## Code map

- `src/sim/` game rules, no graphics: `avatar.ts` (player movement, jumping onto walls, sprint), `ore.ts` (ore nodes, reach), `inventory.ts` (hotbar), `world.ts` (map, walls, no-edge bounds), `pathfinding.ts` (flow field on a blocked grid, 8-way, no corner cutting), `worldgen.ts` (generated world: zones, plateaus, caves, ore, scenery, bare patches), `pieces.ts`, `game.ts` (supply drops, hand, placement rules, towers, combat, credits, HP, waves), `towers.ts` (tower kinds, stats, tuning defaults), `maps.ts`.
- `src/render/` three.js: `models.ts` (named model library, materials, evening palette), `view.ts` (scene, camera, sync with sim, effects), `ship.ts` (the Rocket), `rig.ts` (player rig + animator, `createMultitool`), `ore.ts` (node model), `mining.ts` (mining effects), `icons.ts` (hotbar icons rendered from models), `pieceShape.ts`, `scenery.ts` (world scenery per chunk), `caveLooks.ts`, `cliffLooks.ts`, `enemyLooks.ts`, `alien.ts`.
- `src/input/controller.ts` mouse/keyboard to actions; `src/ui/hud.ts` DOM overlay (hotbar, notices); `src/ui/buildWheel.ts` + `wheel.ts` build wheel; `src/ui/tuning.ts` tuning sliders; `src/main.ts` fixed-step loop (60 ticks/s).
- Every orange part (walls, towers, ship, rig bands) uses `colonyOrange()` from `src/render/palette.ts`: one colour, glow and finish, so all oranges match. Don't create other oranges.
- Rendering uses `THREE.ColorManagement.enabled = false` and legacy-like light intensities to match the mockups.
- Scenery that never moves (trees, rocks, drifts) is merged per material with `bakeStatic` (`src/render/bake.ts`) to keep draw calls low; the World playground went from ~6,000 draw calls zoomed out to ~100. Anything that moves or changes (nodes being mined, walls) stays separate.

## Locked decisions (see design doc for the full list)

- Art style: clean low-poly 3D with three.js, soft light and shadows. Replaces the earlier pixel-art decision.
- Camera: orthographic, fixed iso-style angle (about 30 deg elevation, 45 deg rotation). Pan/zoom OK.
- First world is snowy and cozy: cold world, warm colony (palette: colony orange, cyan power, dark steel, white; violet aliens). Evening lighting is THE look (day/night presets were dropped).
- Game logic never touches graphics; the renderer builds everything from named models so Erik's own models (e.g. Blockbench) can replace placeholders.
- 8-direction movement, no corner cutting. Enemies take the fastest path. No map edge.
- Visual reference: mockups/snow-test.html, Clean 3D view (published at https://claude.ai/artifact/4Y1EHcKtCrum2szcH6Diqd). mockups/look-test.html is the older pixel exploration.

## Pushing

- From Claude Code on Erik's PC: normal `git commit` / `git push` with Erik's own git login.
- From a Claude cloud session (Cowork): the cloud workspace can't push to this repo. Commit and push from the linked PC folder via the device shell, authored as Erik with Claude as co-author, using a fine-grained token Erik provides. Tokens live outside the repo and must never be committed.
